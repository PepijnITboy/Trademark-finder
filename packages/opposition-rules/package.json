{
  "name": "@merkwacht/opposition-rules",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --no-cache --passWithNoTests",
    "test:unit": "vitest run --no-cache --passWithNoTests",
    "test:integration": "echo \"@merkwacht/opposition-rules has no integration tests\""
  },
  "dependencies": {
    "@merkwacht/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
